{
  "name": "aetheria-subgraph",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ aetheria",
    "create-local": "graph create --node http://localhost:8020/ aetheria-local",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 aetheria-local",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.79.2",
    "@graphprotocol/graph-ts": "0.35.1"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}
